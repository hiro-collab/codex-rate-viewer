{
  "name": "codex-rate-viewer",
  "version": "0.1.0",
  "private": true,
  "description": "Realtime local viewer for Codex rate limits.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "pw": "playwright-cli",
    "pw:open": "playwright-cli open http://127.0.0.1:8787"
  },
  "devDependencies": {
    "@playwright/cli": "^0.1.8"
  }
}
